/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/newsletter-sign-up-form",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,
  };

export default nextConfig;
